from fobi.base import FormElementPluginWidget


class BaseTemperatureCPluginWidget(FormElementPluginWidget):
    """BaseTemperatureCPluginWidget."""

    # Same as ``uid`` value of the ``TemperatureCPlugin``.
    plugin_uid = "temperature_c"
